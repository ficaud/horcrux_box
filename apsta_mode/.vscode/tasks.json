{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "🔨 Build (esp32s3_devkitc/procpu)",
            "type": "shell",
            "command": "west build -b esp32s3_devkitc/esp32s3/procpu -d build .",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "clear": true
            }
        },
        {
            "label": "🧹 Pristine Build (esp32s3_devkitc/procpu)",
            "type": "shell",
            "command": "west build -b esp32s3_devkitc/esp32s3/procpu -p always -d build .",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ],
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "clear": true
            }
        },
        {
            "label": "📡 Flash + Monitor depuis host macOS",
            "type": "shell",
            "command": "echo '┌─────────────────────────────────────────────────────────┐\n│ Lancer ces commandes dans un TERMINAL HÔTE macOS        │\n│ (pas dans VS Code, pas dans le container)              │\n├─────────────────────────────────────────────────────────┤\n│                                                         │\n│ 📦 Installer esptool (1 fois) :                        │\n│   pip install esptool                                    │\n│                                                         │\n│ ⚡ Flasher l\\''ESP32-S3 :                                │\n│   esptool.py --chip esp32s3 --port /dev/cu.usbmodem13101 \\''│\n│     --baud 921600 --before default_reset --after hard_reset \\''│\n│     write_flash 0x0 ${workspaceFolder}/build/zephyr/zephyr.bin │\n│                                                         │\n│ 📡 Monitor série :                                      │\n│   screen /dev/cu.usbmodem13101 115200                   │\n│                                                         │\n│ 💡 Le fichier binaire est accessible depuis le host     │\n│    (le workspace est monté dans le container).          │\n│    Build dans VS Code, flash sur le host.               │\n└─────────────────────────────────────────────────────────┘'"
        }
    ]
}